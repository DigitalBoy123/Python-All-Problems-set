angle1 = 60
angle2 = 60
angle3 = 60
def check_triangle():
    if angle1 + angle2 + angle3 == 180: # This is the first method for solving the problem.
        return "Yes the triangle is parellel"
    else:
        return "No the triangle is not parellel"
print(check_triangle())







triangle = 60+60+60 # This is the second method for solving the same problem.
def check_triangle():
    if triangle==180:
        return "Yes the angles are same and the triangle is parallel"
    else:
         return "No the angles are not same and the triangle is not parallel" 
print(check_triangle())